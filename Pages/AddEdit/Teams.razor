@page "/teams/spain"
@inject IRepository db

<div class="spain-teams">
    <TeamList ListOfTeams="teams" />
</div>

@code {

    List<TeamModel> teams;

    protected override void OnInitialized()
    {       
        teams = db.GetTeams().Where(x => x.CountryID == 5).ToList();
    }

}
